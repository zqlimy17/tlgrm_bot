import React, { useState } from "react";

const Users = () => {
    const [user, setUser] = useState([]);
    return (
        <React.Fragment>
            <h1>User Box</h1>
        </React.Fragment>
    );
};

export default Users;
